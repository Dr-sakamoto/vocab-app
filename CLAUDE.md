@AGENTS.md

# Vocab App — コンセプト & 開発指針

## アプリのコンセプト
英単語の日本語訳を**記述式で答える**暗記アプリ。
AIが自由記述の表記ゆれを判定できるようになったことが、このアプリの存在理由。
「集中を切らさずに続けられること（フロー状態）」が最優先。

## このリポジトリに入っている2つのアプリ
- **単語アプリ**（`/`・`/result`、`app/(quiz)/`）— 上に書いたコンセプトはこちらのもの。
  以下のコアループ・禁止事項もすべて単語アプリに対する規定
- **瞬間英作文**（`/compose`、`app/compose/` と `lib/compose/`）— AI採点の英作文・和訳。
  文法タグごとに弱点を測って出題する別アプリで、コアループも学習状態も共有しない。
  こちらの設計方針は [`docs/COMPOSE_APP.md`](./docs/COMPOSE_APP.md) にある
  （1問ずつ添削を読ませるのが学習の本体なので、「正誤をまとめて見せる」規定は当たらない）

## コアバリュー（優先順位順）
1. **フロー状態** — 1問ごとの割り込み・意思決定を増やさない（10問打ち終えるまでは
   画面遷移も含めて何も挟まない。結果発表への遷移は10問の区切りにだけ許容する）
2. **学習効果** — 記述式＋AI判定で「思い出す」負荷を保つ
3. **継続性** — 毎日開きたくなる進行設計（ストリーク・段階的アンロック）
4. **パフォーマンス** — 快適な操作感

## コアループ
10問を一枚の小テストとして出す。日本語訳をタイピングで入力 → Enter で次の設問へ。
確定した回答はその場で裏の採点（完全一致 → AI判定）へ回り、次の設問を打つ時間と
並走する。正誤は1問ずつ出さず、10問ぶんまとめて結果発表で見せる。
結果発表は `/result` への画面遷移として見せ、続けて次のセットへ（`/`）戻る。
クイズの状態はレイアウト直下の Provider に置き、遷移をまたいでも採点中の裏処理や
クラウド同期が途切れないようにする。

## 技術スタック
- Next.js（App Router）
- Supabase（DB・認証）
- Tailwind CSS

## 開発の禁止事項
- 10問の出題中（1問ごと）に割り込みを挟む変更（演出・モーダル・画面遷移の追加）。
  結果発表（`/result`）への遷移は10問の区切りに限って許容する既定の動作であり、これには当たらない
- 10問を打ち終える前に正誤を見せる変更（音・色・カウンタ含む。手が止まる）
- 記述式回答を選択式に置き換える変更（AI判定の存在意義を失わせる）
- 既存テスト（tests/）を壊す変更
- 環境変数・APIキーをクライアントサイドに露出させる実装
- 大規模リファクタリング（スコープを絞った小さな改善のみ許可）

## 自律AIエージェントへの追加指示
- 毎回必ずテストを実行して全パスを確認してからPRを作成する
- 1PRにつき1つの改善に絞る（複数混在させない）
- 迷ったら「何もしない」を選ぶ（壊すくらいなら手を付けない）
- モンスター育成・世界観・ストーリーは本アプリのスコープ外。別プロジェクト
  （EMG）へ分離する方針。指示がない限りこの話題を自発的に提起しない
