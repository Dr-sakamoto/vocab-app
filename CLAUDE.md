@AGENTS.md

# Vocab App — コンセプト & 開発指針

## アプリのコンセプト
英単語の日本語訳を**記述式で答える**暗記アプリ。
AIが自由記述の表記ゆれを判定できるようになったことが、このアプリの存在理由。
「集中を切らさずに続けられること（フロー状態）」が最優先。

## コアバリュー（優先順位順）
1. **フロー状態** — 1問ごとの割り込み・意思決定・画面遷移を増やさない
2. **学習効果** — 記述式＋AI判定で「思い出す」負荷を保つ
3. **継続性** — 毎日開きたくなる進行設計（ストリーク・段階的アンロック）
4. **パフォーマンス** — 快適な操作感

## コアループ
10問を一枚の小テストとして出す。日本語訳をタイピングで入力 → Enter で次の設問へ。
確定した回答はその場で裏の採点（完全一致 → AI判定）へ回り、次の設問を打つ時間と
並走する。判定は採点が返った設問から順に、その行に出る（随時公開）。
手はもう次の設問に移っているので待ちにはならず、誤答の正解訳がその場に残るため
遡って見直す必要もない。結果発表はセット全体の評価を出す場で、
画面遷移せず中身だけ入れ替わり、続けて次のセットへ。

判定を出す条件は「その設問の採点が返ったこと」だけ。採点の完了を待たせた瞬間に
1問1答へ戻るので、そこは越えない。

## 技術スタック
- Next.js（App Router）
- Supabase（DB・認証）
- Tailwind CSS

## 開発の禁止事項
- コアループに割り込みを挟む変更（演出・モーダル・画面遷移の追加）
- 採点の完了を待たせる変更（判定を待ってから次へ進ませる形。手が止まる）
- 判定を回答行の外へ広げる変更（設問行の色分け・ヘッダーの正解カウンタ・
  1問ごとの効果音。打っている行から視線とリズムを引き剥がす）
- 出題中に、打っている行を画面の別の位置へ動かす変更
  （上の行に判定が生えたぶんは、必ずスクロールで打ち消す）
- 記述式回答を選択式に置き換える変更（AI判定の存在意義を失わせる）
- 既存テスト（tests/）を壊す変更
- 環境変数・APIキーをクライアントサイドに露出させる実装
- 大規模リファクタリング（スコープを絞った小さな改善のみ許可）

## 自律AIエージェントへの追加指示
- 毎回必ずテストを実行して全パスを確認してからPRを作成する
- 1PRにつき1つの改善に絞る（複数混在させない）
- 迷ったら「何もしない」を選ぶ（壊すくらいなら手を付けない）
- モンスター育成・世界観・ストーリーは本アプリのスコープ外。別プロジェクト
  （EMG）へ分離する方針。指示がない限りこの話題を自発的に提起しない
